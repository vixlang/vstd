pub fn rand(mini:i32,max:i32) ->i32 //tips:由左值决定返回值大小，如下文的n是i32，所以返回值也是i32
{
    let seed = 12345
    seed = (seed * 9301 + 49297) % 233280
    if (seed < 0) {
        seed = -seed
    }
    return mini + (seed % (max - mini + 1))
}//共有函数
