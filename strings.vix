pub fn strcmp(a:ptr,b:ptr) ->i32
{
    let i = 0
    while(1)
    {
        let ca = a[i]
        let cb = b[i]
        if (ca != cb)
        {
            return ca - cb
        }
        if (ca == 0)
        {
            return 0
        }
        i +=1
    }
}
